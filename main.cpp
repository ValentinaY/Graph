#include <iostream>
#include "Graph.h"

using namespace std;
int main(){

	Graph<char> grafo;

/* 
	Formato para el uso de los métodos:
	T vert1;
	T vert2;
	addvertex(type);
	connectvertex(vert1, vert2, 10);
	long n=getnvertex();
	deletevertex( vert1 );
	deleteedge(vert1, vert2, 10);
	int nv = hasedge(vert1, vert2);
	draw("file.txt");
	dikstra(vert1, vert2);
*/

	return 0;
}